---
controller_roles:
  - credentials:
      - "{{ orgs }} dev Machine Ansible Privileged"
    team: "ldap-org02-users"
    role: use
  - credentials:
      - "{{ orgs }} dev Machine Ansible Privileged"
      - "{{ orgs }} dev Machine Ansible Non-Privileged"
    team: "ldap-org02-devs"
    role: use
...
